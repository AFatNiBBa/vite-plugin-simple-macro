{
  "private": true,
  "workspaces": [
    "package/*"
  ],
  "scripts": {
    "build": "npm run build -w package/vite-plugin-simple-macro && npm run build -w package/vite-macro-tanstack-server-func",
    "clean": "git clean -Xdf"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.58.9",
    "@typescript/typescript6": "^6.0.2",
    "unplugin-dts": "^1.0.3",
    "vite": "^8.1.3"
  }
}
